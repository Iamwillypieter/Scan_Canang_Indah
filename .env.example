# ─── Environment Mode ────────────────────────────────────────
# Copy file ini menjadi .env lalu sesuaikan mode-nya.
#
# production  = koneksi ke mesin fingerprint asli via LAN
# development = pakai dummy data lokal (tidak perlu jaringan)
APP_MODE=production
